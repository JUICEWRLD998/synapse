{
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps && npx kendo-ui-license activate && next build",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
